#include <stdio.h>

int main() {
    int a = 6;
    int b = 5;
    printf("before swapping: a = %d, b = %d\n", a,b);
    // swapping comndition
    int temp;
    temp = a;
    a = b;
    b = temp;
    
    printf("after swapping: a = %d, b = %d\n", a,b);
    
}
